# Default Manifest
# Each line is a path (relative to agent-loop root) to load into context.
# Hot memory (memory/hot/) is ALWAYS loaded automatically — don't list it here.
# Lines starting with # are ignored.

# Warm memory
memory/warm/current-priorities.md

# Test task
projects/example-project
/task.md
